<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple code</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quara Accounts</h1>

<% for(post of posts) { %>
<div class="post">
  <h3 class="user">@<%= post.username %></h3>
  <h4 class="content"><%= post.content %></h4>
  <div class="post-actions">
    <a class="details-link" href="http://localhost:5000/post/<%= post.id %>">See in details</a>
    <a class="edit-link" href="http://localhost:5000/post/<%= post.id %>/edit">Edit</a>
    <form method="post" action="/post/<%= post.id %>?_method=DELETE">
      <button class="delete-button">Delete post</button>
    </form>
  </div>
</div>
<% } %>
<div class="anker">
  <a href="http://localhost:5000/post/new">create new</a>
</div>

   
</body>
</html>